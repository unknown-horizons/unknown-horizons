from openanno.rootcommand import RootCommand

class BuildRootCommand(RootCommand):
	def __init__(self, pos, building, playerid, world = None):
		self.pos = pos
		self.building = building
		self.playerid = playerid
		self.world = world
	
	def execute(self):
		#world.build(pos, building, playerid)
		print "Haus bauen!"
	
	def can_execute(self):
		return True
from openanno.rootcommand import RootCommand

class BuildRootCommand(RootCommand):
	def __init__(self, pos, building, playerid, world = None):
		self.pos = pos
		self.building = building
		self.playerid = playerid
		self.world = world
	
	def execute(self):
		#world.build(pos, building, playerid)
		print "Haus bauen!"
	
	def can_execute(self):
		return True
from openanno.rootcommand import RootCommand

class BuildRootCommand(RootCommand):
	def __init__(self, pos, building, playerid, world = None):
		self.pos = pos
		self.building = building
		self.playerid = playerid
		self.world = world
	
	def execute(self):
		#world.build(pos, building, playerid)
		print "Haus bauen!"
	
	def can_execute(self):
		return True
from openanno.rootcommand import RootCommand

class BuildRootCommand(RootCommand):
	def __init__(self, pos, building, playerid, world = None):
		self.pos = pos
		self.building = building
		self.playerid = playerid
		self.world = world
	
	def execute(self):
		#world.build(pos, building, playerid)
		print "Haus bauen!"
	
	def can_execute(self):
		return True
from openanno.rootcommand import RootCommand

class BuildRootCommand(RootCommand):
	def __init__(self, pos, building, playerid, world = None):
		self.pos = pos
		self.building = building
		self.playerid = playerid
		self.world = world
	
	def execute(self):
		#world.build(pos, building, playerid)
		print "Haus bauen!"
	
	def can_execute(self):
		return True