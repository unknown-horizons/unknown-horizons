sudo: required
os:osx

before_install:
  - brew update

install:
  - brew outdated python3 || brew upgrade python3 || brew install python3
  - brew install swig sdl2 sdl2_image sdl2_ttf freetype openal-soft tinyxml glew libvorbis || brew outdated swig sdl2 sdl2_image sdl2_ttf freetype openal-soft tinyxml glew libvorbis || brew install swig sdl2 sdl2_image sdl2_ttf freetype openal-soft tinyxml glew libvorbis
  - brew outdated git || brew install git || brew upgrade git
  - pwd
